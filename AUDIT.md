# Fresh recovery audit — 2026-09-14

Recovered 11 original source modules containing 104 named theorem/lemma declarations, including private lemmas and finite specializations. The generated root import module is counted separately in the machine-readable inventory. This is a lexical count, not a count of independent results or kernel-accepted theorems.

Sorry/admit tokens outside comments/strings: 0. Explicit axiom declarations: 0. See [per-file declarations and SHA-256 hashes](audit/source-inventory.json). `native_decide` occurs in the historical sources and is not equivalent to a kernel-only proof replay.

A fresh compiler attempt is saved in [compiler-attempt.json](audit/compiler-attempt.json). Mathlib is not installed in this isolated project, so this attempt cannot certify a clean build. No successful build or kernel coverage is claimed. Run the README build commands to install the pinned dependencies and perform the complete replay.

The old 74-theorem / 8-module headline is superseded by this explicit source inventory. The general first-cover permutation/bijection theorem is still open in the recovered packet.
